// Riot Night public comments (optional).
// To enable a shared, anonymous comment board:
// 1) Create a Firebase project with Realtime Database.
// 2) Paste your config object below (from Firebase console).
// 3) Set DB rules to allow read/write (or tighten later).
// If left blank, the site falls back to local-only comments.

window.__FIREBASE_CONFIG__ = {
  apiKey: "",
  authDomain: "",
  databaseURL: "",
  projectId: "",
  appId: ""
};
