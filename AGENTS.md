# AI Tool Entry

Read `.ai/RULES.md` and `.ai/STATE.md` before working, then `CLAUDE.md`.
The shared project rules live in `.ai/RULES.md`; current state in `.ai/STATE.md`.

Live client site (static, branch `master`). **Git is NOT live** — live is a MANUAL Netlify deploy and the git origin is divergent/stale. Do not push expecting a deploy, do not deploy, do not submit real contact/lead forms, do not read `.env`/secrets, and do not change DNS/billing without the required gate (clear, scoped confirmation from David). Do not touch the superseded repos / sibling Netlify projects listed in `.ai/STATE.md`.
